using System;

namespace Task9_Rectangle_n_Circle
{
    class Task9_Rectangle_n_Circle
    {
        static void Main()
        {
            //Console.Write("Value for x: ");
            //double x = double.Parse(Console.ReadLine());
            //Console.Write("Value for y: ");
            //double y = double.Parse(Console.ReadLine());

            //if ((Math.Pow(x - 1.1, 2) + Math.Pow(y - 1.1, 2) < Math.Pow(3, 2)) && (y > 1))
            //{
            //    Console.WriteLine("The point is within the circle and outside the rectanle");
            //}

            int a = 0x10;
            int b = 0x2;
            Console.WriteLine(a);
            Console.WriteLine(b);
        }
    }
}
